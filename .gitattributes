# Normalize all text files to LF in the repo and on checkout (consistent
# across Windows/macOS/Linux; silences the CRLF conversion warnings).
* text=auto eol=lf

# Windows batch files must keep CRLF if any are ever added to the repo.
*.cmd text eol=crlf
*.bat text eol=crlf
